# 1. Install Homebrew
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# 2. Install chezmoi
brew install chezmoi

# 3. Init chezmoi and apply dotfiles
chezmoi init --apply https://github.com/oskar/dotfiles.git
